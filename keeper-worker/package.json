{
  "name": "decant-keeper-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Decant keeper as a Cloudflare Cron Worker (funding settlement + liquidations).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.52.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
